{
  "tests": [
    {
      "name": "Check Telegram OAuth redirect",
      "method": "GET",
      "path": "/?provider=telegram",
      "expectedStatus": 302
    },
    {
      "name": "Check Yandex OAuth (without secrets)",
      "method": "GET",
      "path": "/?provider=yandex",
      "expectedStatus": 400,
      "expectedBody": {
        "error": "string"
      },
      "bodyMatcher": "partial"
    }
  ]
}